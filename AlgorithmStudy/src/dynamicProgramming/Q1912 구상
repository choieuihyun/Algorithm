n개의 정수로 이루어진 임의의 수열에서 연속된 몇 개의 수를 선택해서
구할 수 있는 합 중 가장 큰 합을 구하려고 한다. 단, 수는 한 개 이상 선택해야한다.

- 입력받은 N의 크기만큼 배열이 생성될 것이다.

- N개의 인덱스 중에서 연속된 몇 개의 수를 선택하는 구조임.

- 첫번째 인덱스 기준으로 생각해보자

	1. 10 -4 3 1 5 6 -35 12 21 -1을 입력받았음.
	
	2. 첫번째 인덱스인 10을 기준으로 나머지 9개 연산, 8개 연산, 7개 연산 이런 식으로 줄여나감.
	
		2-1. 9개를 연산한 값은 seqSumMemo[0]에 저장.
		
		2-2. 8개를 연산한 값은 seqSumMemo[1]에 저장.
		
		2-3. seqSumMemo[]에 저장된 값중 최댓값 추출하고 초기화.
	
	3. 그다음 두번째 인덱스인 -4를 기준으로 나머지 8개 연산, 7개 연산 이런 식으로 줄여나감.
	
		3-1. 위처럼 똑같이 seqSumMemo[]에 저장된 값중 최댓값 추출.
	
	4. 어떻게 보면 그냥 백트래킹인데 이 과정에서 메모이제이션과 탐색하지 않은 인덱스의 조건을 거는
	   과정만 다른게 아닐까?
	   
- 그러면 필요한거. 정수 N, N의 크기를 가지는 배열, 메모용 배열


뭐 비슷한 생각이었지만 틀렸다.

정답은 max로 (memo값 + array값) , (array값) 비교해가면서 탐색하는거였다.

아니 정답보러가면 뭐 쉬운 문제였다 이러는데 어려운데 뭐가 쉽다는거야.